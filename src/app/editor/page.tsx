import { FC } from "react";
import Editor from "@/features/editor/components/Editor";

const EditorPage: FC = () => {
  return (
    <div>
      <Editor />
    </div>
  );
};

export default EditorPage;
